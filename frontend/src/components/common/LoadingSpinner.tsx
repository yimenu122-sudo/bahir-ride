import React from 'react';

const LoadingSpinner: React.FC = () => (
  <div className="loading-screen">
    <div className="spinner"></div>
    <span>Loading Bahir-Ride...</span>
  </div>
);

export default LoadingSpinner;
